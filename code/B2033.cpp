#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b;
    cin >> a >> b;
    long long sum = (long long)a * (long long)b;
    cout << sum;
    //cout << fixed << setprecision(3) << d;
}
